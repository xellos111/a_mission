# A-Mission 모듈 개발 로그

## 1. 개요
이 문서는 **A-Mission (커뮤니티 미션 & 보상 모듈)** 개발 과정에서 발생한 주요 이슈와 해결책, 그리고 향후 계획을 정리한 문서입니다.

## 2. 주요 시행착오: 모듈 설치 및 액션 등록 오류

### 2.1 문제 상황
- 모듈을 서버에 업로드하고 관리자 페이지에 접근했으나, **"잘못된 요청입니다" (Invalid Request)** 또는 **403 Forbidden** 오류가 지속적으로 발생.
- `module.xml` 설정이 DB(`rx_module_actions`)에 제대로 반영되지 않는 현상.

### 2.2 원인 분석 과정
1.  **PHP 문법 및 설정 파일 확인**: `php -l` 검사 및 `module.xml` 문법 검사를 수행했으나 이상 없음.
2.  **DB 테이블 스캔**: 자동 설치가 실패하여 수동으로 데이터를 넣으려 했으나, 표준 테이블인 `rx_module_actions`가 존재하지 않음.
3.  **접두사(Prefix) 확인**: `rx_` 접두사를 사용 중임을 확인했지만 여전히 테이블을 찾을 수 없음.
4.  **전체 테이블 스캔 (Scan DB)**: DB 내 모든 테이블을 스캔하여 '액션' 관련 컬럼을 가진 테이블을 탐색.

### 2.3 최종 원인
- 해당 라이믹스/XE 환경은 표준적인 `rx_module_actions` 테이블 대신, **구형(또는 특정 버전 호환) 테이블인 `rx_action_forward` 테이블을 사용**하고 있었음.
- 자동 설치 스크립트나 코어의 모듈 업데이트 로직이 이 테이블 구조 차이를 일시적으로 처리하지 못하거나, 초기 설정(`ruleset` 오류 등)으로 인해 꼬여있었음.

### 2.4 해결책 (Solution)
- **`fix_install_action_forward_v2.php`** 스크립트를 제작하여 실행.
- DB 컬럼(`act`, `module`, `type`)을 정확히 타겟팅하여 `rx_action_forward` 테이블에 필수 액션(`dispAMissionAdminConfig` 등)을 강제로 삽입(Insert).
- 이후 정상적으로 관리자 페이지 접근 가능.

---

## 3. 앞으로의 개발 방향 (Roadmap)

### 3.1 1단계: 관리자 기능 안정화 (현재 단계)
- [x] 모듈 설치 및 설정 페이지 진입 정상화.
- [ ] 미션 생성/수정/삭제 기능 완벽 검증.
- [ ] 다양한 트리거(로그인, 글쓰기 등) 조건 설정 UI 개선.

### 3.2 2단계: 핵심 로직 검증
- [ ] **트리거 연동**: 실제 유저 활동 시 미션 달성 여부 체크(`checkMission`)가 정상 작동하는지 테스트.
- [ ] **캐싱 효율성**: `getActiveMissionsByTrigger` 캐싱이 부하를 얼마나 줄여주는지 확인.
- [ ] **티켓 지급**: 미션 완료 시 포인트/티켓이 정확히 지급되고 로그(`rx_a_mission_tickets`)에 남는지 확인.

### 3.3 3단계: 게임 기능 및 프론트엔드
- [ ] **게임 UI**: 유저가 티켓을 소모해 게임을 즐길 수 있는 직관적인 UI 페이지(`dispAMissionGame`) 제작.
- [ ] **게임 로직 확장**: 현재의 단순 확률 게임 외에 다양한 미니게임 로직 추가.
- [ ] **대시보드**: 유저가 자신의 미션 현황과 티켓 내역을 볼 수 있는 마이페이지 대시보드 구현.

---

**작성일**: 2026-02-05
**작성자**: Antigravity (AI Assistant)
